#include<stdio.h>

/*
2.	求两个字符串的编辑距离规则如下：
   执行一次删除操作，编辑距离加1；
   执行一次插入操作，编辑距离加1；
   修改一个字符，编辑距离加1；
   例如testing修改为kestan的编辑距离为3，用C语言书写，添加注释，并计算复杂度

 */

int main(int argc, char const *argv[])
{
    char *str1 = "testing";
    char *str2 = "kestan";

    
    printf("%s",str1);
    return 0;
}
